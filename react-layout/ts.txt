натяжка верстки на реакт(инет магаз)

МАКЕТ И РЕЗУЛЬТАТ ДОЛЖЕН БЫТЬ ОДИНАКОВЫМ!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
богдан пж сделай по плану а не по тупому

структура проекта:

    1) делаем один файл(index.html)
    2) разрезаем весь css на мелкие файлы
        (выбррать только той цсс который относится к странице (5 страниц - 5 цсс)) 
    3) js - весь перенести в компоненты...( выбирать толкьо нужный)
    4) весь код хтмл по классам не может повторятсья, а должен быть компонентом (не div classname container, а <Container/>)

порядок работы: 
    1) открывем index.html и отмечаем все одинковые классы
        начиная с родительских до доч.
        (хедер и др. можно по компонентам, а подключить в app.js)
        и перевести их в компонент
    2) все елем мелкие button img input icon title text - компоненты
    3) слайдер карусель и все блоки которым нужно двигаться - компоненты з нарезкой js
    4) разбиваем всё на мелкие задачи 
    5) САМОЕ ВАЖНОЕ - тестирование на каждом шагу // при каждом подключение каждого компонента

    1) html 
    2) css
    3) js